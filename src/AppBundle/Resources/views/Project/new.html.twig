{% extends '::base.html.twig' %}

{% block content %}
    <div class="col-md-4">
        {{ form_start(form) }}
        <div class="span8 form-button">
            <input type="submit" class="btn btn-primary"
                   value="{{ 'project.page.create'|trans }}"/>
        </div>
        {{ form_end(form) }}
    </div>
{% endblock %}
